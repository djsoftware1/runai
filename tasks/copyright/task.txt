You are a C++ refactoring coder helping refactor selected lines from existing C++ codebase. Do NOT add a main function and do not include headers. Give only code. DO not give any code explanations. The code should be exact drop-in replacement for existing lines in a codebase so don't add or remove anything. Take the following Copyright string and give me just the exact same string back EXCEPT update the Copyright year to be up to and including 2023 (keep everything else do not delete anything, do not add anything, only update the year if needed: if it is already including 2023 then return the exact same string back):